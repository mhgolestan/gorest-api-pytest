name: API Tests with Allure Report

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Validate secrets
      env:
        BASE_URL: ${{ secrets.BASE_URL }}
        TEST_USER_TOKEN: ${{ secrets.TEST_USER_TOKEN }}
      run: |
        echo "BASE_URL set: ${BASE_URL:+yes}"
        echo "TEST_USER_TOKEN set: ${TEST_USER_TOKEN:+yes}"
        echo "BASE_URL value: $BASE_URL"
        echo "TEST_USER_TOKEN value: $TEST_USER_TOKEN"
        if [ -z "$BASE_URL" ] || [ -z "$TEST_USER_TOKEN" ]; then
          echo "ERROR: Please set repository secrets BASE_URL and TEST_USER_TOKEN"; exit 1
        fi

    - name: Debug Python Settings
      env:
        BASE_URL: ${{ secrets.BASE_URL }}
        TEST_USER_TOKEN: ${{ secrets.TEST_USER_TOKEN }}
      run: python3 -c "from settings import base_settings; print('base_url:', repr(base_settings.base_url)); print('token_len:', len(base_settings.test_user_token)); print('api_url:', repr(base_settings.api_url))"

    - name: Run tests with Allure
      env:
        BASE_URL: ${{ secrets.BASE_URL }}
        TEST_USER_TOKEN: ${{ secrets.TEST_USER_TOKEN }}
      run: python3 -m pytest --alluredir=./allure-results -n auto --tb=short -x

    # - name: Generate Allure Report
    #   if: always()
    #   uses: simple-elf/allure-report-action@master
    #   with:
    #     allure_results: allure-results
    #     gh_pages: gh-pages
    #     allure_history: allure-history

    # - name: Deploy report to GitHub Pages
    #   if: always()
    #   uses: peaceiris/actions-gh-pages@v4
    #   with:
    #     github_token: ${{ secrets.GITHUB_TOKEN }}
    #     publish_dir: allure-report

    # - name: Upload Allure Report as Artifact
    #   if: always()
    #   uses: actions/upload-artifact@v4
    #   with:
    #     name: allure-report
    #     path: allure-report
    #     retention-days: 30
